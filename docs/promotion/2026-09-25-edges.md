# Candidate 2: pinning replay, exception and capacity gates

Status: **scoped gates passed; overall promotion remains HOLD**.

The exact installed candidate2 pinning binary reproduced the historical known pin
at sequence 2147507508 / locktime 1497688167 in a one-position replay. The separate
application CPU exporter reproduced the exact public parameter blob and its verifier
accepted the returned record. This is a historical replay, not a fresh full search;
the spent fixture was not assembled, signed, submitted or mined.

Three diagnostic derivatives were compiled from the current subset source lock and
flags. Their unmodified-build receipt matches installed subset `1c7d6b59…`. No release
solver source or predicate was changed. Diagnostic injection is deliberately separate:

- Device hit counts 0, 1, 63 and 64 completed with exact record counts. Counts 65,
  1024 and 1025 failed closed before publication or completion. Buffer canaries were
  intact; retained records were unique and their indices matched exact ranked
  combinations. The 1025 case retained only the device buffer's 1024 records and
  correctly rejected the range. This is synthetic device-hit pressure, not natural
  DER discoveries or support for completing overflowing ranges.
- Public scalar values 0, 7 and n−7 with neg_r_inv=1 and u2R=7G exercised exceptional
  recovery. Forced handoff with 7 and 64 active lanes and actual detector behavior
  with 7 active lanes produced **156 independently matching branches**, including
  finite flags and compressed keys. Inactive tail lanes completed. Forced count65
  failed closed. Scalars were injected; no naturally occurring SHA preimages are
  claimed. Real DER filtering remained active and produced no output hits.

The first batch rejected a harness error: merged stderr interrupted a stdout
record during the 1024 case. Its pod was deleted without crediting the full gate.
The corrected runner separates streams; a focused regression reproduces that
interleaving safely. The corrected full batch passed and its result hash was verified
before independent CPU checking. [Receipt](2026-09-25-edges.json) records identities,
counts, fixture relation and scope. No compute-sanitizer run is claimed by this batch.

Both pods were deleted and zero remaining pods confirmed before stopping their
watchdogs. Live price announced for each allocation was $0.74/hour plus storage.
Historical proof endpoints and release defaults remain unchanged.

Remaining: final-build memory-check evidence, final independent approval, fresh
wallet-backed full-predicate withdrawal proof with unmodified Core, and reviewed
normal combined-release publication/enrollment. External miner/mainnet certification
remains separate. These diagnostics cannot replace the fresh proof.

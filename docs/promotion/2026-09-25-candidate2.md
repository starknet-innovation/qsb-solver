# Repaired candidate: installed-image regression

Status: **HOLD — not promoted for application use**.

[Candidate 2](https://github.com/starknet-innovation/qsb-solver/releases/tag/candidate-20260925-2)
was published from `bef76bf9aec123d95fdff53fb839a0c44f378927` by
[workflow 36119510144](https://github.com/starknet-innovation/qsb-solver/actions/runs/36119510144).
The verified immutable image is
`ghcr.io/starknet-innovation/qsb-solver@sha256:9e86d94a66d7893e31d8e8d6bec7ddbc9b59f47f09e2f7a6015679fd95ab8b52`.
GitHub provenance matched the exact image, source, candidate tag and workflow.

The installed repaired pinning binary is
`4602c9845d7db1336b5ad00d67348063dce2c164bed3005ba18624f8c3dc6fa7`,
matching the [native host-error audit](2026-09-25-pinning-repair.md).
The subset binary remains
`1c7d6b5906e95c12f07faf08f93cfe5ca920974c9cb936909548b81b82dade3a`.
The independent reviewer checked the native fault traces and closed the original
three pinning findings for the tested SLOTPIPE=0/sm89 configuration. This does not
constitute overall release approval.

## Executed checks

One RTX 4090 ran the immutable OCI image with the committed regression harness.
The actual installed handler completed its configured pinning range and final
round-1/round-2 ranges with synthetic nonexistent outpoints and zero candidate
files. Returned stage, commit, manifest, attempt and exact range identities matched.
This uses the real handler in child processes; it is not a fresh serverless queue
transport test or new durable coverage credit.

Twenty SegWit/Taproot, both-round cases cover rank zero, small counts, block-edge
starts, a large interior rank and the exact end of the combinatorial domain. Both
the installed subset executable and a source-locked trace derivative completed each
case. The separate application CPU reference checked exact rank/recid multiplicity,
full transaction sighashes and recovered-key hashes: **3,116 candidates / 6,232
hashes matched**. The trace derivative preserves predicates but is a different
executable; the exact installed binary was separately run on the same bounded
inputs. Neither check proves exhaustive correctness over the whole search domain.

Both historical single-candidate subset hit replays matched expected records using
the exact installed subset binary. These are replay evidence, not fresh searches,
new wallet signatures, transaction spends or proof of final withdrawal.

The [machine-readable receipt](2026-09-25-candidate2.json) records executable,
source, reference, trace and result identities and individual case counts. The
public validation artifact is attached to the candidate prerelease. All paid test
resources were deleted; a fresh provider list showed zero pods before watchdog
termination. Price announced before allocation: $0.74/hour plus storage.

## Still required

- Exact normal pinning known-hit verification against the independent CPU reference.
- Source/final-build-bound subset exception and capacity evidence, with diagnostic
  injection distinguished from naturally discovered events.
- Fresh full-predicate final-image wallet-backed withdrawal proof and unmodified
  Core verification. Old fixtures are spent and cannot be reused.
- Reviewed normal combined-release descriptor/publication and application enrollment.

Production release defaults, historical endpoints and mainnet remain unchanged.
External miner certification is a separate uncompleted gate.
